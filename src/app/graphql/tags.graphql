query taxonomyTermTagQuery {
  taxonomyTermQuery(offset: 0, limit: 5) {
    tags: entities {
      ... on TaxonomyTermTags {
        name
        tid
        url: entityUrl {
          path
        }
      }
    }
  }
}
