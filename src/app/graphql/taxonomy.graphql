query taxonomyTermQuery {
  taxonomyTermQuery(offset: 0, limit: 15) {
    entities {
      ... on TaxonomyTermTheme {
        name
        tid
        url: entityUrl {
          path
        }
      }
    }
  }
}
